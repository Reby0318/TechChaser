#Various ways to calculate pay
hrs = input("Enter Hours:")
hrs = float(hrs)
rate = input("per hour:")
rate = float(rate)
pay = hrs * rate
print ("Pay:", pay)

xh = input("Enter Hours:")
xr = input("Enter Rate: ")
xp = float(xh) * float(xr)
print ("Pay:",xp)

hrs = float(input("Enter Hours:"))
rate = float(input("per hour:"))
pay = hrs * rate
print ("Pay:", pay)

hrs = float(input("Enter Hours:"))
rate = float(input("per hour:"))
print ("Pay:", hrs * rate)

pay = float(input("Enter Hours:"))*float(input("per hour:"))
print ("Pay:", pay)

print ("Pay:", float(input("Enter Hours:"))*float(input("per hour:")))